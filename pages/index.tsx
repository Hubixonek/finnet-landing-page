import LayoutPresenter from "../components/LayoutPresenter";
const HomePage = () => {
  return (
    <LayoutPresenter>
      <h1>Finnet Page </h1>
    </LayoutPresenter>
  );
};

export default HomePage;
