import styles from "../../styles/FirstSection.module.scss";

const FirstSection = () => {
  return (
    <section className={styles["section"]}>
      <div className={styles["curve"]}>Pierwsza sekcja</div>
    </section>
  );
};

export default FirstSection;
